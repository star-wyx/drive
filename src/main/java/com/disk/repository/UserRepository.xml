<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.disk.repository.UserRepository">

    <select id="queryUserNamePwd" parameterType="map" resultType="User">
        select user_id, user_name, user_pwd, user_email from netdisk.users where user_name=#{user_name} and user_pwd=#{user_pwd} limit 1;
    </select>

    <select id="queryUserEmailPwd" parameterType="map" resultType="User">
        select user_id, user_name, user_pwd, user_email from netdisk.users where user_email=#{user_email} and user_pwd=#{user_pwd} limit 1;
    </select>

    <select id="queryUserByName" parameterType="map" resultType="User">
        select user_id, user_name, user_pwd, user_email from netdisk.users where user_name=#{user_name} limit 1;
    </select>

    <select id="queryUserByEmail" parameterType="map" resultType="User">
        select user_id, user_name, user_pwd, user_email from netdisk.users where user_email=#{user_email} limit 1;
    </select>



    <insert id="insertUser" parameterType="map">
        insert into netdisk.users values(null,#{user_name},#{user_pwd},#{user_email})
    </insert>

    <select id="queryDataByUserId" parameterType="Integer" resultType="String">
        select content from netdisk.users_data where user_id = #{user_id}
    </select>

</mapper>